{
  "name": "be-api",
  "version": "0.1.0",
  "main": "src/server.js",
  "type": "module",
  "private": false,
  "scripts": {
    "dev": "node --watch src/server.js",
    "start": "node src/server.js",
    "lint": "echo \"(add eslint later)\" && exit 0",
    "test": "echo \"(add tests later)\" && exit 0"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "helmet": "^7.1.0",
    "pino": "^9.4.0",
    "pino-http": "^10.3.0"
  }
}
